server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location /health {
    default_type application/json;
    return 200 '{"status":"ok"}';
  }

  location / {
    try_files $uri $uri/ =404;
  }
}
