# Local dev container (docker-compose.yml). Not read by Bzync Cloud itself —
# see README.md for the variables the platform injects for a linked database.

MYSQL_VERSION=8.0
MYSQL_DATABASE=app
MYSQL_USER=app
MYSQL_PASSWORD=changeme
MYSQL_PORT=3306
