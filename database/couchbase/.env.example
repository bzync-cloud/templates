# Passed to `docker run --env-file .env`. Not read by Bzync Cloud itself —
# see README.md for the variables the platform injects for a linked database.

COUCHBASE_VERSION=
COUCHBASE_BUCKET=app
COUCHBASE_USERNAME=app
COUCHBASE_PASSWORD=changeme
