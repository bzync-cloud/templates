CRON_SCHEDULE="*/5 * * * *"

# Port for the status JSON endpoint (see job.js) — this is what Bzync
# Cloud's health check and public dashboard URL hit, not the cron
# scheduler itself (which has no port).
STATUS_PORT=3000
