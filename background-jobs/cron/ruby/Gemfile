source "https://rubygems.org"

ruby "~> 3.4"

gem "rufus-scheduler"
gem "dotenv"
# Removed from Ruby's default gems in 3.0+ — needed explicitly for job.rb's
# status HTTP server (see BZYNC_CLOUD).
gem "webrick"
