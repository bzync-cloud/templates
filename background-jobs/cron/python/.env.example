CRON_INTERVAL_SECONDS=300

# Port for the status JSON endpoint (see job.py) — this is what Bzync
# Cloud's health check and public dashboard URL hit, not the cron loop
# itself (which has no port).
STATUS_PORT=3000
