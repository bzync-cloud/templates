WORKER_INTERVAL_MS=30000

# Port for the status JSON endpoint (see worker.js) — this is what Bzync
# Cloud's health check and public dashboard URL hit, not the worker loop
# itself (which has no port).
STATUS_PORT=3000
