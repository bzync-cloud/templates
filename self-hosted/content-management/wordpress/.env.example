# Environment variables for this project.
# Set these in the Bzync Cloud dashboard under each environment.
#
# Database connection variables (MYSQL_HOST, MYSQL_DATABASE, MYSQL_USER,
# MYSQL_PASSWORD) are injected automatically when a MySQL database is linked
# to this environment in the dashboard.

PORT=8080
