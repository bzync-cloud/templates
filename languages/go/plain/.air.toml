root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/main"
  cmd = "go build -o ./tmp/main ."
  delay = 1000
  exclude_dir = ["tmp", "testdata"]
  include_ext = ["go"]
  kill_delay = "0s"
  stop_on_error = false
